import { APIGatewayEvent } from "aws-lambda";
import { expect } from "chai";
import { SinonStub } from "sinon";
import { helloWorld } from "../ApiEndpoint";


describe("HelloWorld Api Endpoint Tests", () => {
    it("returns 200 on successful calls", (done) => {


    });

    it("returns 400 on unsuccessful calls", (done) => {

    });
});